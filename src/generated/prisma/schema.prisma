generator client {
  provider     = "prisma-client-js"
  moduleFormat = "cjs"
  output       = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Example Model
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
